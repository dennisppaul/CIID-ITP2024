int myvalue = 23;

String mName = "Dennis P Paul";

// complex data types usually start with a captial letter
// complex data are also referred to as classes

void setup() {
  size(1024, 768);
  println(mName); // print string
  println(mName.length()); // print length of string
  println(mName.substring(0, 6)); // just print "Dennis"
}
